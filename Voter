#include<iostream>
using namespace std;
int main()
{
    int age;
    cout<<"Enter the age : ";
    cin>>age;
    if(age >= 18)
    cout<<"\nThe person is eligible to vote.";
    else
    cout<<"\nThe person is not eligible to vote.";
    return 0;
}
